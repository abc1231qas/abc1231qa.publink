# 東方禪意極簡風格設計指南

## 📋 專案概述
本文檔記錄了 `abc1231qa.publink` 網站的視覺風格改造，從現代極簡風格轉變為**東方禪意極簡 (Zen Minimalism)** 風格。

---

## 🎨 核心設計理念

### 1. 氛圍定位
- **東方禪意極簡**：大量留白，強調「空」的概念
- **人文知性**：類似誠品書店的沉穩、深度閱讀體驗
- **靜謐神聖**：透過對稱構圖與低飽和度營造內省空間感

### 2. 目標受眾感受
- 平靜與專注
- 降低視覺噪音
- 放慢閱讀速度，進入思考模式
- 傳遞「探討真理」的信賴感

---

## 🎨 配色系統 (墨與金)

### 主要色彩變數
```css
:root {
  --bg-rice: #F7F7F5;           /* 米白背景 (Rice Paper) */
  --ink-black: #2C2C2C;         /* 墨黑主色 (Charcoal Black) */
  --text-deep: #333333;         /* 深灰文字 (標題) */
  --text-mid: #595959;          /* 中灰文字 (內文) */
  --gold-muted: #C5A065;        /* 霧金輔助色 (Antique Gold) */
  --gold-light: rgba(197, 160, 101, 0.15); /* 淡金背景 */
}
```

### 色彩應用原則
| 元素 | 色彩 | 用途 |
|------|------|------|
| 背景 | `#F7F7F5` | 模擬紙張質感，避免純白刺眼 |
| 主要筆觸 | `#2C2C2C` | 水墨元素、深色文字 |
| 強調元素 | `#C5A065` | 金色圓圈、分隔線、hover 狀態 |
| 標題文字 | `#333333` | 確保可讀性 |
| 內文文字 | `#595959` | 長時間閱讀不疲勞 |

---

## ✍️ 字體系統

### 中文字體
```css
font-family: 'Noto Serif TC', 'PMingLiU', serif;
```
- **主要字體**：思源宋體 (Noto Serif TC)
- **備用字體**：細明體 (PMingLiU)
- **禁用字體**：黑體 (Sans-serif) - 會破壞禪意

### 英文字體
- Times New Roman
- Garamond
- Baskerville

### 字體層級
- **標題 (h1)**：`2.8rem`, `font-weight: 300`, `letter-spacing: 0.15em`
- **副標題**：`1rem`, `letter-spacing: 0.3em`
- **內文**：`1.05rem`, `line-height: 2`

---

## 🖼️ 視覺元素

### 1. 水墨圓圈 (Enso)
```css
.enso {
  width: 180px;
  height: 180px;
  border: 3px solid var(--ink-black);
  border-radius: 50%;
  opacity: 0.85;
  transform: rotate(-15deg);
  border-top-color: transparent;
  border-right-color: transparent;
}
```
- 象徵禪宗「圓相」
- 中心點使用金色光暈
- 不完整的圓形營造手繪感

### 2. 金色分隔線
```css
.divider {
  width: 60px;
  height: 1px;
  background: linear-gradient(90deg, transparent, var(--gold-muted), transparent);
}
```

### 3. 極簡按鈕
- 細邊框設計 (`1px solid`)
- Hover 時金色邊框 + 淡金背景
- 平滑過渡動畫 (`cubic-bezier(0.4, 0, 0.2, 1)`)

---

## 📐 版面配置

### 居中對稱佈局
```css
.zen-container {
  max-width: 680px;
  text-align: center;
}
```

### 留白比例
- **極度寬鬆**：留白佔比約 50%-60%
- 元素間距：60px - 100px
- 內文區塊最大寬度：480px

### 響應式設計
```css
@media (max-width: 640px) {
  h1 { font-size: 2rem; }
  .enso { width: 140px; height: 140px; }
}
```

---

## ✨ 動畫效果

### 漸入動畫
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 動畫時序
1. Enso 圓圈：`0.3s` 延遲
2. 標題：`0.6s` 延遲
3. 副標題：`0.9s` 延遲
4. 分隔線：`1.2s` 延遲
5. 內文：`1.5s` 延遲
6. 連結：`1.8s` 延遲
7. 頁尾：`2.1s` 延遲

---

## 📄 頁面清單

### 1. 首頁 (`/`)
- **標題**：靜觀 (CONTEMPLATION)
- **核心元素**：水墨圓圈、金色分隔線
- **內容**：
  ```
  於喧囂中尋一方淨土
  在代碼裡悟人生哲理
  技術與人文的交匯處
  即是心之所向
  ```

### 2. 404 頁面
- **標題**：迷途 (Lost)
- **核心元素**：大型 404 數字 (opacity: 0.15)
- **內容**：
  ```
  此處無路可循
  不如返回原點
  ```

### 3. 管理後台 (`/admin`)
- 保持原有功能性設計
- 未來可考慮套用禪意風格

---

## 🎯 設計原則總結

### ✅ 應該做的
- 使用宋體/明體字型
- 保持大量留白
- 使用低飽和度色彩
- 居中對稱佈局
- 細線條裝飾元素
- 漸進式動畫

### ❌ 避免做的
- 使用黑體字型
- 填滿所有空間
- 使用高飽和度色彩
- 複雜的不對稱佈局
- 粗重的視覺元素
- 突兀的動畫效果

---

## 🖼️ Midjourney 生成 Prompt

如需生成符合風格的背景圖或配圖：

```
Minimalist Zen presentation background, distinct ink wash brush stroke style blended with divine golden kintsugi details. A vast negative space composition on high-quality texture rice paper (Washi). Visual elements: a subtle antique gold circle or arc, minimal elegant layout. Lighting is soft, diffused, and ethereal. Color palette: Off-white background (#F7F7F5), charcoal black ink (#2C2C2C), and muted metallic gold (#C5A065). High-end editorial design aesthetics, spiritual and intellectual atmosphere, 8k resolution, serene mood --ar 16:9 --style raw --v 6.0
```

---

## 📝 實作檔案

- **主要檔案**：`worker.js`
- **修改函數**：
  - `generateIntroHTML()` - 首頁
  - `generate404HTML()` - 404 頁面
- **字體來源**：Google Fonts (Noto Serif TC)

---

## 🔄 未來優化方向

1. **圖片資源**：加入更多水墨風格的裝飾圖片
2. **互動效果**：滑鼠移動時的微妙視差效果
3. **音效**：可考慮加入禪意背景音樂（可選）
4. **深色模式**：設計符合禪意的深色主題
5. **管理後台**：將後台介面也改為禪意風格

---

**設計完成日期**：2026-02-01  
**設計師**：Antigravity AI  
**風格靈感**：東方禪宗美學 × 現代極簡主義
